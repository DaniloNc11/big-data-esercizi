name: redis
version: "3.9"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  redisinsight:
    image: redislabs/redisinsight:latest
    ports:
      - "8001:8001"
    volumes:
      - redisinsight:/db
    depends_on:
      - redis

volumes:
  redisinsight:
